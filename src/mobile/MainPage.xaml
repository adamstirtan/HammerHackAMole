<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="mobile.MainPage"
    Title="HammerHack-A-Mole">

    <VerticalStackLayout Padding="20">
        <Label x:Name="ScoreLabel" Text="Score: 0" />
        <Grid RowSpacing="20" ColumnSpacing="20" Margin="0,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Frame Grid.Row="0" Grid.Column="0">
                <ImageButton x:Name="MoleButton0" Clicked="OnMoleClicked" BackgroundColor="Transparent" />
            </Frame>
            <Frame Grid.Row="0" Grid.Column="1">
                <ImageButton x:Name="MoleButton1" Clicked="OnMoleClicked" BackgroundColor="Transparent" />
            </Frame>
            <Frame Grid.Row="0" Grid.Column="2" >
                <ImageButton x:Name="MoleButton2" Clicked="OnMoleClicked" BackgroundColor="Transparent" />
            </Frame>
            <Frame Grid.Row="1" Grid.Column="0">
                <ImageButton x:Name="MoleButton3" Clicked="OnMoleClicked" BackgroundColor="Transparent" />
            </Frame>
            <Frame Grid.Row="1" Grid.Column="1">
                <ImageButton x:Name="MoleButton4" Clicked="OnMoleClicked" BackgroundColor="Transparent" />
            </Frame>
            <Frame Grid.Row="1" Grid.Column="2">
                <ImageButton x:Name="MoleButton5" Clicked="OnMoleClicked" BackgroundColor="Transparent" />
            </Frame>
            <Frame Grid.Row="2" Grid.Column="0">
                <ImageButton x:Name="MoleButton6" Clicked="OnMoleClicked" BackgroundColor="Transparent" />
            </Frame>
            <Frame Grid.Row="2" Grid.Column="1">
                <ImageButton x:Name="MoleButton7" Clicked="OnMoleClicked" BackgroundColor="Transparent" />
            </Frame>
            <Frame Grid.Row="2" Grid.Column="2">
                <ImageButton x:Name="MoleButton8" Clicked="OnMoleClicked" BackgroundColor="Transparent" />
            </Frame>
        </Grid>
    </VerticalStackLayout>

</ContentPage>
